
#digits = len(str(n))
#ID

#n = 123456789
#digits = len(str(n))
#print(digits)

a = 123456789
f = len(str(a))
id = 702790071
b = "Print your ID \n"


while True:
    c = int(input(b))
    if (c) == id:
        print("Correcto")
        break

    #if int(c) < id:
        #print("Incorrecto, a el ID introducido le faltan numeros")

    if (c) > id: False
        #print("Incorrecto, el ID introducido tiene numeros de mas.")
    if (c) < id: False
        #print("Incorrecto, a el ID introducido le faltan numeros")


    # Esta funcion tiene que responder: (La cantidad de numeros del ID esta bien pero el ID no es correcto) si el usuario introdujo los 9 numeros del ID.
    #if (c) == : 
        #print("La cantidad de numeros del ID esta bien pero el ID no es correcto")
        #print(f)
        # 
    s = "La cantidad de numeros del ID esta bien pero el ID no es correcto"

    def bro():
      while s:
        if int(c) == f:
          print(f)
          return(s)
    bro()
    